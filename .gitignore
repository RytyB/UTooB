# Ignore Gradle project-specific cache directory
.gradle

# Ignore Gradle build output directory
build

# Dummy check to make sure secrets don't slip in with the build
client_secret.json
userPrefs.json
apiKey.txt